{
  "mcpServers": {
    "knowledge": {
      "type": "http",
      "url": "${KB_BASE_URL:-http://127.0.0.1:12098}/api/v1/mcp",
      "headers": {
        "Authorization": "Bearer ${KB_MCP_TOKEN}"
      }
    }
  }
}
