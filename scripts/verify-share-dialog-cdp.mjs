const ts=await fetch('http://127.0.0.1:9223/json/list').then(r=>r.json());const t=ts.find(x=>x.url.includes('/w/')&&x.url.includes('/n/'));if(!t)throw new Error('note page missing');const ws=new WebSocket(t.webSocketDebuggerUrl);let id=0;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){const q=p.get(m.id);p.delete(m.id);m.error?q.j(m.error):q.r(m.result)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise((r,j)=>{const n=++id;p.set(n,{r,j});ws.send(JSON.stringify({id:n,method,params}))});await c('Runtime.enable');const ex=async x=>(await c('Runtime.evaluate',{expression:x,returnByValue:true})).result.value;const clicked=await ex(`(()=>{const b=[...document.querySelectorAll('button')].find(x=>x.textContent.trim()==='分享');if(!b)return false;b.click();return true})()`);await new Promise(r=>setTimeout(r,400));const dialog=await ex("document.querySelector('[role=dialog]')?.innerText||''");const ok=clicked&&dialog.includes('分享《')&&dialog.includes('生成并复制')&&dialog.includes('还没有分享链接');console.log(JSON.stringify({ok,dialog},null,2));ws.close();if(!ok)process.exitCode=1;
