import http from'node:http';const server=http.createServer(async(req,res)=>{let b='';for await(const x of req)b+=x;const j=JSON.parse(b||'{}');res.setHeader('content-type','application/json');if(req.url.endsWith('/embeddings')){const inputs=Array.isArray(j.input)?j.input:[j.input];res.end(JSON.stringify({data:inputs.map((s,index)=>{const seed=typeof s==='string'?s:JSON.stringify(s??'');const v=[0,0,0,0];for(let i=0;i<seed.length;i++)v[seed.charCodeAt(i)%4]+=1;const norm=Math.hypot(...v)||1;return{index,embedding:v.map(x=>x/norm)}})}));}else res.end(JSON.stringify({choices:[{message:{content:'根据知识库，答案是测试通过 [#1]。'}}],usage:{prompt_tokens:12,completion_tokens:8}}));});server.listen(19091,'127.0.0.1',()=>console.log('mock-ai-ready'));