const ts=await fetch('http://127.0.0.1:9223/json/list').then(r=>r.json());const t=ts.find(x=>x.url.includes('127.0.0.1:5174'));if(!t)throw new Error('no page');const ws=new WebSocket(t.webSocketDebuggerUrl);let id=0;const p=new Map();ws.onmessage=e=>{const m=JSON.parse(e.data);if(m.id&&p.has(m.id)){const q=p.get(m.id);p.delete(m.id);m.error?q.j(m.error):q.r(m.result)}};await new Promise((r,j)=>{ws.onopen=r;ws.onerror=j});const c=(method,params={})=>new Promise((r,j)=>{const n=++id;p.set(n,{r,j});ws.send(JSON.stringify({id:n,method,params}))});await c('Runtime.enable');const ex=async x=>(await c('Runtime.evaluate',{expression:x,returnByValue:true,awaitPromise:true})).result.value;const out=await ex(`(()=>{const header=document.querySelector('header');const main=document.querySelector('main.app-grid');const aside=document.querySelector('aside');const styles=[...document.styleSheets].map(s=>({href:s.href,rules:(()=>{try{return s.cssRules.length}catch{return -1}})()}));const pick=e=>e?{display:getComputedStyle(e).display,height:getComputedStyle(e).height,width:getComputedStyle(e).width,bg:getComputedStyle(e).backgroundColor,border:getComputedStyle(e).borderBottomWidth,padding:getComputedStyle(e).padding}:null;return{url:location.href,styles,header:pick(header),main:pick(main),aside:pick(aside),root:getComputedStyle(document.documentElement).getPropertyValue('--background'),body:document.body.innerText.slice(0,500)}})()`);console.log(JSON.stringify(out,null,2));ws.close();
