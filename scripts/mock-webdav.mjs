import http from'node:http';const files=new Map;const s=http.createServer(async(req,res)=>{let b=[];for await(const x of req)b.push(x);const data=Buffer.concat(b);if(req.method==='PUT'){files.set(req.url,data);res.statusCode=201;res.end();}else if(req.method==='DELETE'){files.delete(req.url);res.statusCode=204;res.end();}else if(req.method==='GET'&&req.url==='/_files'){res.setHeader('content-type','application/json');res.end(JSON.stringify([...files].map(([path,data])=>({path,bytes:data.length,head:data.subarray(0,6).toString()}))));}else{res.statusCode=404;res.end();}});s.listen(19092,'127.0.0.1',()=>console.log('mock-webdav-ready'));